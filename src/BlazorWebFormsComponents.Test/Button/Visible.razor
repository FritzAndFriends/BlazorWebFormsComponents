@inherits BunitContext

@code {

	[Fact]
	public void Button_VisibleFalse_RendersNothing()
	{
		// Arrange & Act
		var cut = Render(@<Button Text="Click Me!" Visible="false" OnClientClick="window.navigate('bing.com')" />);

		// Assert
		cut.MarkupMatches(string.Empty);
	}

}
