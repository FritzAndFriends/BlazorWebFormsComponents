@using static BlazorWebFormsComponents.WebColor
@using BlazorWebFormsComponents

@code {
	[Fact]
	public void ImageButton_Style_RendersBackColorAndForeColor()
	{
		var cut = Render(@<ImageButton ImageUrl="/img/VB.png" AlternateText="Blue Button" BackColor="Blue" ForeColor="White" />);

		cut.Find("input").HasAttribute("style").ShouldBeTrue();
		var style = cut.Find("input").GetAttribute("style");

		style.ShouldNotBeNull();
		style.ShouldContain("background-color:Blue");
		style.ShouldContain("color:White");
	}
}
