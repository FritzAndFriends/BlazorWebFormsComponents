@inherits BunitContext

@code {
    [Fact]
    public void DataList_TableLayout_Tooltip_RendersCorrectly()
    {
        var cut = Render(@<DataList Items="Widget.SimpleWidgetList"
                                    ItemType="Widget"
                                    RepeatLayout="Table"
                                    ToolTip="This is my tooltip"
                                    Context="Item">
            <HeaderTemplate>My Widget List</HeaderTemplate>
            <ItemTemplate>@Item.Name</ItemTemplate>
        </DataList>);

        cut.Find("table").HasAttribute("title").ShouldBeTrue();
        cut.Find("table").GetAttribute("title").ShouldBe("This is my tooltip");
    }
}
