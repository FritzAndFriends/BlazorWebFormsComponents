@inherits BaseStyledComponent
@typeparam Type

@if (Enabled && Visible)
{
	<span style="@($"{DisplayStyle}{CalculatedStyle}")">
		@if (string.IsNullOrWhiteSpace(Text))
		{
			@ErrorMessage
		}
		else
		{
			@Text
		}
	</span>
}
