// PLACEHOLDER: Replace this file with the real Chart.js v4.4.8 bundle.
// Download from: https://cdn.jsdelivr.net/npm/chart.js@4.4.8/dist/chart.umd.min.js
// This stub provides a minimal Chart constructor so the component can load without errors.

(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
	typeof define === 'function' && define.amd ? define(factory) :
	(global = typeof globalThis !== 'undefined' ? globalThis : global || self, global.Chart = factory());
})(this, (function () {
	'use strict';

	class Chart {
		constructor(ctx, config) {
			this.ctx = ctx;
			this.config = config;
			this.data = config.data || {};
			this.options = config.options || {};
			console.warn('Chart.js PLACEHOLDER: Replace wwwroot/js/chart.min.js with the real Chart.js v4.4.8 bundle.');
		}

		update() {
			// Placeholder - no-op
		}

		destroy() {
			// Placeholder - no-op
		}
	}

	Chart.register = function () {
		// Placeholder - no-op
	};

	return Chart;
}));
