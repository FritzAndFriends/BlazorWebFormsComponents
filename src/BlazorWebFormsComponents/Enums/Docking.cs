namespace BlazorWebFormsComponents.Enums;

/// <summary>
/// Specifies where a chart element is docked. Matches the ASP.NET Web Forms Docking enum.
/// </summary>
public enum Docking
{
	Top = 0,
	Right = 1,
	Bottom = 2,
	Left = 3
}
