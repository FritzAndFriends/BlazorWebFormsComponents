namespace BlazorWebFormsComponents.Enums;

/// <summary>
/// Specifies the selection mode for the ListBox control.
/// </summary>
public enum ListSelectionMode
{
	/// <summary>
	/// Only a single item can be selected at a time.
	/// </summary>
	Single,

	/// <summary>
	/// Multiple items can be selected at the same time.
	/// </summary>
	Multiple
}
