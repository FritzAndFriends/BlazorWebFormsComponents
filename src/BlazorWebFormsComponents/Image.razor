@inherits BaseWebFormsComponent

@if (Visible)
{
	if (string.IsNullOrEmpty(ToolTip))
	{
		<img src="@ImageUrl" alt="@AlternateText" longdesc="@DescriptionUrl" />
	}
	else
	{
		<img src="@ImageUrl" alt="@AlternateText" longdesc="@DescriptionUrl" title="@ToolTip" />
	}
}
