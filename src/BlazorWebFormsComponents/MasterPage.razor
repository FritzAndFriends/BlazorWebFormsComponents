@inherits MasterPageBase
@layout EmptyLayout

@if (!string.IsNullOrEmpty(PageTitle))
{
	<PageTitle>@PageTitle</PageTitle>
}

@if (Head != null)
{
	<HeadContent>
		@Head
	</HeadContent>
}

@if (Visible)
{
	@ChildContent
}
