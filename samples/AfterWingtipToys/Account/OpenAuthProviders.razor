@* TODO(bwfc-identity): OpenAuthProviders requires external auth provider configuration.
   Configure external providers in Program.cs (e.g., AddGoogle, AddFacebook) and update this component. *@
<div id="socialLoginList">
    <h4>Use another service to log in.</h4>
    <hr />
    <div>
        <p>There are no external authentication services configured.</p>
    </div>
</div>
